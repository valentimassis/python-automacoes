[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "windows-file-server-auditor"
version = "0.1.0"
description = "Auditoria automatizada de servidores de arquivos Windows"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]